# テスト仕様書：English Reading Quest

## 1. テスト対象

- `app.py`（Flaskサーバー／APIバックエンド）
- `static/index.html`（Vue.jsフロントエンドUI）

## 2. テスト環境

- Windows 10／11
- Python 3.11以降
- Flask、openai、python-dotenv
- Gemini APIキー設定済み、またはOllama起動済み
- ブラウザ：Google Chrome、Microsoft Edge
- データベース：使用しない（サーバーのメモリおよびブラウザのセッションストレージを使用）

## 3. テスト項目

### 3.1 サーバー起動・静的ファイル配信

| No | テスト内容 | 手順 | 期待結果 |
|---:|---|---|---|
| 1 | サーバー起動 | `python app.py`を実行 | エラーなく起動し、`http://localhost:5000/`へアクセスできる |
| 2 | index.html配信 | ブラウザで`/`へアクセス | English Reading Questのホーム画面が表示される |
| 3 | デバッグ設定 | `FLASK_DEBUG=false`で起動 | デバッグモードが無効になる |

### 3.2 問題生成API 正常系

対象：`POST /api/v1/question-sets`

| No | テスト内容 | 手順 | 期待結果 |
|---:|---|---|---|
| 4 | 初級問題生成 | `level: beginner`と文章形式を送信 | HTTP 201で問題セットが返る |
| 5 | 中級問題生成 | `level: intermediate`と文章形式を送信 | HTTP 201で問題セットが返る |
| 6 | 上級問題生成 | `level: advanced`と文章形式を送信 | HTTP 201で問題セットが返る |
| 7 | 問題構成 | 生成JSONを確認 | 設問が2問あり、各設問に重複しない選択肢が4つある |
| 8 | 英文の長さ | `passage`の英単語数を確認 | 130～200語である |
| 9 | 正解番号 | `correct_choice`を確認 | 0～3の整数である |
| 10 | 根拠 | `evidence`を本文内で検索 | 本文に完全一致で含まれる |
| 11 | 日本語項目 | 日本語訳と解説を確認 | 日本語として表示できる内容である |
| 12 | 文章形式 | メール、通知、広告、記事を指定して生成 | 指定した形式に対応した英文が返る |

### 3.3 問題生成API 異常系

| No | テスト内容 | 手順 | 期待結果 |
|---:|---|---|---|
| 13 | JSONなし | 本文なしで送信 | HTTP 400が返る |
| 14 | 難易度なし | `level`を省略 | HTTP 422が返る |
| 15 | 不正な難易度 | 未定義の難易度を送信 | HTTP 422が返る |
| 16 | 不正な文章形式 | 未定義の形式を送信 | HTTP 422が返る |
| 17 | APIキー未設定 | GeminiのAPIキーを外して生成 | 設定またはAI接続エラーとなり、秘密情報は表示されない |
| 18 | AI出力形式不正 | AIが不正JSONを返す状態を再現 | 再生成後も失敗した場合はHTTP 502が返る |
| 19 | AIタイムアウト | AI応答が規定時間を超える状態を再現 | HTTP 504と再試行案内が返る |
| 20 | 連続操作 | 制限を超えて生成を連続実行 | HTTP 429と待機案内が返る |

### 3.4 回答提出・採点API

対象：`POST /api/v1/question-sets/{question_set_id}/submissions`

| No | テスト内容 | 手順 | 期待結果 |
|---:|---|---|---|
| 21 | 全問正解 | 2問とも正解を送信 | HTTP 201、得点`2 / 2` |
| 22 | 一部正解 | 1問のみ正解を送信 | HTTP 201、得点`1 / 2` |
| 23 | 全問不正解 | 2問とも不正解を送信 | HTTP 201、得点`0 / 2` |
| 24 | 採点内容 | 結果JSONを確認 | 正誤、正解、根拠、日本語解説が返る |
| 25 | 未回答 | 1問分だけ送信 | HTTP 422が返る |
| 26 | 番号範囲外 | `selected_choice`に-1または4を指定 | HTTP 422が返る |
| 27 | 存在しない問題 | 存在しないUUIDを指定 | HTTP 404が返る |
| 28 | 不正なID | UUIDでないIDを指定 | HTTP 400または404となり、サーバーは停止しない |

### 3.5 詳細説明API

対象：`POST /api/v1/question-sets/{question_set_id}/explanations`

| No | テスト内容 | 手順 | 期待結果 |
|---:|---|---|---|
| 29 | 詳細説明取得 | 採点後に本文中の文を選択 | HTTP 200で文法・フレーズの日本語説明が返る |
| 30 | 本文外の文 | 本文にない文を送信 | HTTP 422が返る |
| 31 | 採点前 | 採点前に説明を要求 | HTTP 409が返る |
| 32 | 2回目 | 同じ問題セットで再度要求 | HTTP 409となり、1回制限が守られる |

### 3.6 タイムアタックAPI

対象：`POST /api/v1/time-attacks`、`POST /api/v1/time-attacks/{time_attack_id}/answers`、`POST /api/v1/time-attacks/{time_attack_id}/finish`

| No | テスト内容 | 手順 | 期待結果 |
|---:|---|---|---|
| 33 | 開始 | ホームからタイムアタック開始 | HTTP 201で最初の問題が表示される |
| 34 | 出題数 | 最後まで進める | 中級5パッセージ、合計10問が出る |
| 35 | 形式 | 複数回開始して確認 | メールだけに固定されず対応形式から選ばれる |
| 36 | 次へ | 現在の2問に回答 | 回答が保存され、次のセットが返る |
| 37 | 前へ | 「前へ」を押す | 以前の問題と選択済み回答を確認できる |
| 38 | 全問終了 | 5セットに回答 | 10問の得点と解説を確認できる |
| 39 | 時間超過 | 開始から10分後に回答 | 受理されずゲームオーバーになる |
| 40 | 手動終了 | 制限時間内に終了 | その時点の回答が採点される |
| 41 | 終了後回答 | 終了済みIDへ回答 | HTTP 409が返る |
| 42 | 不明なID | 存在しないIDを指定 | HTTP 404が返る |
| 43 | 詳細説明 | 結果画面で各パッセージについて要求 | 5パッセージそれぞれ1回まで取得できる |

### 3.7 UIテスト

| No | テスト内容 | 手順 | 期待結果 |
|---:|---|---|---|
| 44 | ホーム | `/`を開く | 通常学習、タイムアタック、進捗、メニューが表示される |
| 45 | 学習設定 | 通常学習を選択 | 難易度と文章形式を選択できる |
| 46 | Loading | 問題生成を開始 | 背景がぼけ、Loadingとキャンセルが表示され、他操作が無効になる |
| 47 | キャンセル | 生成中にキャンセル | 通知を出さず直前画面へ戻る |
| 48 | 未回答警告 | 一部未回答で採点 | 警告し、採点しない |
| 49 | 結果表示 | 2問に回答して採点 | 得点、正誤、根拠、解説が表示される |
| 50 | 根拠表示 | 採点結果を確認 | 根拠箇所が視覚的に分かる |
| 51 | 日本語訳 | 日本語訳を表示 | 英文に対応する訳が表示される |
| 52 | 詳細説明UI | 本文の文を初めてクリック | 確認後に取得し、閉じた後も同じ説明を再表示できる |
| 53 | 経過時間 | 通常問題を開始 | 経過時間が表示される |
| 54 | 進捗更新 | 問題に回答 | 回答数、正答率、ランク進捗が更新される |
| 55 | 初期正答率 | 新しいセッションで開く | 累計正答率が`-`になる |
| 56 | タブ終了 | 学習後にタブを閉じて開き直す | セッションストレージの進捗が初期化される |
| 57 | エラー表示 | 各エラーを発生させる | 状況別の日本語メッセージが表示され、内部情報は出ない |
| 58 | ブラウザ | ChromeとEdgeで主要操作 | 表示崩れなく生成・回答・採点できる |

## 4. その他の確認項目

- 各生成項目の最大長を超えたAI出力が拒否されること
- 条件違反のAI出力は規定回数まで再生成されること
- タイムアタック生成の途中で失敗しても、成功済み問題を残して失敗箇所から再試行すること
- 同一タイムアタック内で問題文や設問が過度に重複しないこと
- 同時リクエストでも回答や現在位置が二重更新されないこと
- APIキー、Authorizationヘッダー、プロンプト全文がログに出ないこと
- 一定時間を超えた古いデータがメモリから削除されること
- 生成、採点、詳細説明、タイムアタック開始の所要時間を計測すること
- AI通信の異常系は、必要に応じてモックで再現すること

---

実施時は、実施日、実施者、実際の結果、合否、不具合がある場合のIssue番号を別途記録する。
